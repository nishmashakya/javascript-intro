// function with 2 args, return concatenated string

function combineStrings(string1, string2) {
	return string1 + " " + string2
}

let firstName = "Peter";
let lastName = "Parker";

alert(combineStrings(firstName, lastName))